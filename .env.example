# WeldQAi Environment Variables
# Copy this to .env and fill in your values
# NEVER commit .env to version control

# ─── Stripe ──────────────────────────────────────────────────────────────────
# Live keys start with sk_live_ / pk_live_
# Test keys start with sk_test_ / pk_test_
STRIPE_SECRET_KEY=sk_live_your_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_secret_here

# Stripe Price IDs (from your Stripe Dashboard → Products)
STRIPE_PRICE_ONE_REPORT=price_1Sb9c7Cfx756A1WKgYbqLXMU
STRIPE_PRICE_FIVE_REPORTS=price_1Sb9ceCfx756A1WKdQed1rHE
STRIPE_PRICE_MONTHLY=price_1Sb9dTCfx756A1WKzbGaQli4

# ─── App URLs ─────────────────────────────────────────────────────────────────
# For mobile: use a custom URL scheme (e.g. weldqai://payment-success)
# For web: use your hosted domain
APP_SUCCESS_URL=https://your-real-domain.com/payment-success
APP_CANCEL_URL=https://your-real-domain.com/payment-cancel
APP_BILLING_RETURN_URL=https://your-real-domain.com/account

# ─── Firebase (Cloud Functions secrets — set via Firebase CLI) ────────────────
# These are deployed with: firebase functions:secrets:set STRIPE_SECRET_KEY
# Do NOT put real Firebase API keys here — they live in firebase_options.dart
# and google-services.json (client-side public config, not secrets)

# ─── Sentry ──────────────────────────────────────────────────────────────────
# Create a project at https://sentry.io and get the DSN from
# Settings → Projects → <project> → Client Keys (DSN)
# Pass to build: --dart-define=SENTRY_DSN=https://...@sentry.io/...
SENTRY_DSN=https://your_key@o0.ingest.sentry.io/your_project_id

# ─── Admin ───────────────────────────────────────────────────────────────────
# If you add server-side admin logic requiring a specific email
FIREBASE_ADMIN_EMAIL=your_admin_email_here
