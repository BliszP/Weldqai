# Flutter
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Obfuscation / symbolication
app.*.symbols
app.*.map.json

# Android Studio build artifacts
/android/app/debug
/android/app/profile
/android/app/release
android/build/

# Orphan lockfile (no corresponding package.json at root)
/package-lock.json

# Firebase — CRITICAL: contains project config and API keys
# These files are generated by FlutterFire CLI and google-services plugin.
# Retrieve them from your team's secure secret store (e.g. 1Password, Vault,
# CI environment secrets) — never commit to a public repository.
google-services.json
GoogleService-Info.plist
firebase_options.dart
flutterfire.json

# Firebase web config — contains API key; injected at build/CI time from GitHub Secret
web/firebase-config.js

# Android signing — NEVER commit keystore or signing config
android/key.properties
android/app/upload-keystore.jks
android/app/*.jks
android/app/*.keystore

# Environment & Secrets — CRITICAL
.env
.env.local
.env.production
.env.staging
*.env

# Firebase local tooling
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
storage.rules.bak
database-debug.log
*-debug.log

# Cloud Functions dependencies
functions/node_modules/
functions/.env
functions/lib/

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Miscellaneous
*.iml
*.ipr
*.iws
.history
.svn/
.swiftpm/
migrate_working_dir/
.atom/
.build/
.buildlog/

# Test artifacts
coverage/
*.lcov

# Archive folder — dead code graveyard, not for production builds
archive/
